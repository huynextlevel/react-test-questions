import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    paddingLeft: 10,
  },
  weatherDetailRow: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10,
  },
}));
